You are working on my Laravel 13 AI Email Assistant inbox.

IMPORTANT:
- DO NOT modify the left conversation list/panel.
- The left conversation list is already stable and must remain exactly as it is.
- Do not refactor unrelated code.
- Do not change database architecture.
- Do not add unnecessary dependencies.
- Reuse the existing Bootstrap 5 + Sneat UI and existing services/routes where possible.
- Before changing code, inspect the current inbox/thread implementation and understand how GHL messages are currently loaded and sent.

TASK:

1. FIX MESSAGE / THREAD PANEL
Make the middle Message panel stable and behave like GoHighLevel's conversation/message view.

Required:
- Display the full conversation/message thread correctly.
- Messages must appear in the correct chronological order.
- Clearly distinguish customer messages and agent messages.
- Keep the message area internally scrollable without causing the whole page to scroll.
- Composer must remain usable at the bottom.
- Reply must work correctly.
- The agent must be able to choose WHICH specific message they are replying to, similar to GHL.
- When clicking Reply on a specific message:
  - show the selected/referenced message above the composer or in a reply-preview area
  - clearly show who/what message is being replied to
  - allow canceling the reply
  - sending must send the reply to the correct conversation/thread
- Do not implement a fake frontend-only reply. Connect it to the existing Laravel/GHL flow.
- Preserve existing polling/realtime behavior if already implemented.
- Avoid duplicate messages when polling.
- Avoid the thread jumping unexpectedly when new messages arrive.
- Handle loading, empty state, and error state properly.

2. ADD THIRD COLUMN
Change the inbox layout to 3 columns:

LEFT:
Conversation List
- DO NOT TOUCH THIS.
- Keep the current implementation exactly as it is.

CENTER:
Message / Conversation Thread
- This is the panel you must fix in task #1.

RIGHT:
Profile / Contact Details
- Add a new third column on the right side.
- For now, make it an EMPTY placeholder only.
- Do not implement profile functionality yet.
- Give it a clean Bootstrap/Sneat layout with a simple "Contact Details" header.
- It should occupy a stable fixed-width column on desktop.
- On smaller screens, follow the existing responsive/offcanvas behavior if appropriate.

TARGET LAYOUT:

┌────────────────────┬──────────────────────────────┬───────────────────┐
│                    │                              │                   │
│ Conversation List  │ Message / Conversation      │ Contact Details   │
│                    │ Thread                       │                   │
│ DO NOT MODIFY      │ FIX THIS                    │ EMPTY FOR NOW     │
│                    │                              │                   │
│                    │                              │                   │
│                    │ Composer                     │                   │
└────────────────────┴──────────────────────────────┴───────────────────┘

IMPORTANT IMPLEMENTATION RULES:
- Use Bootstrap utility classes wherever possible.
- Avoid creating custom CSS unless absolutely necessary.
- Do not use min-vh-100 inside nested inbox panels if it causes page-level scrolling.
- The inbox should fit within the available layout/page height.
- Only the conversation list and message thread should scroll internally where necessary.
- The right profile column should not create an additional page-level scrollbar.
- Do not modify the existing conversation-list component.
- Do not modify unrelated navbar/sidebar/footer/layout code unless absolutely required to make the 3-column inbox work.
- Do not remove existing GHL functionality.
- Do not break existing star/read/polling/message functionality.

WORKFLOW:
1. Inspect the existing inbox Blade files, controllers, services, routes, and JS related to:
   - conversation list
   - conversation thread
   - sending messages
   - message polling
   - GHL API
2. Identify the minimum files that need changes.
3. Implement the 3-column layout.
4. Fix the middle message/thread panel.
5. Implement message-specific Reply behavior.
6. Add the empty right Contact Details panel.
7. Test for:
   - desktop layout
   - responsive behavior
   - opening conversations
   - loading messages
   - replying to a specific message
   - sending reply
   - polling/new messages
   - no duplicate messages
   - no unwanted page-level scrolling

IMPORTANT:
Do NOT rewrite the entire inbox.
Do NOT redesign the left conversation list.
Do NOT touch the left panel unless a tiny integration change is absolutely required.

At the end:
- Give a concise summary of files changed.
- Explain exactly how message-specific Reply works.
- Mention any remaining limitation.